TinyLink Frontend

Setup:
1. cd frontend
2. npm install
3. npm run dev

The frontend expects the backend API under the same origin (e.g., run a proxy or run both apps under the same host). Adjust API base URLs if needed.
